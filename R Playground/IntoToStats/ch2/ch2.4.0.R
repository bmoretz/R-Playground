filePath <- file.path(getwd(), "Data/thusen.txt")

# quick export
write.table(thuesen, filePath, sep = "\t")

thusen2 <- read.table(filePath, header = T)

thusen2

secretin

levels(secretin$time)

system.file("rawdata", package = "ISwR")

read.table(filePath, colClasses = c(numeric, NULL))

aq <- edit(airquality)

dd <- data.frame()
fix(dd)

dd

read.table("clipboard", header = T)